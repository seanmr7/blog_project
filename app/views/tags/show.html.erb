<h1>Articles Tagged with <%= @tag.name.capitalize %></h1>

<ul>
  <% @tag.articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %></li>
  <% end %>
</ul>
<br />
<%= link_to "Delete", tag_path(@tag), data: {confirm: 'Delete this tag?'}, method: :delete %>